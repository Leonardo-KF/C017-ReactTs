export type Product = {
  id: string;
  name: string;
  description: string;
  price: number;
  imageURL: string;
};

export type ProductInput = {
  name: string;
  description: string;
  price: number;
  imageURL: string;
};
